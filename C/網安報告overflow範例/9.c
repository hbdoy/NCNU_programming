#include<stdio.h>

int main(){
    int a[2]={1,2};
    int b[2]={3,4};
    /*
    a[0]=1;
    a[1]=2;
    b[0]=3;
    b[1]=4;
    */
    printf("a[0]=%d\na[1]=%d\nb[0]=%d\nb[1]=%d\n\n",a[0],a[1],b[0],b[1]);
    //如果我刻意存取陣列外的元素，例如int b[3]，會發生什麼事呢？
    printf("let b[3]=9\n\n");
    b[3]=9;
    printf("a[0]=%d\na[1]=%d\nb[0]=%d\nb[1]=%d\n\n",a[0],a[1],b[0],b[1]);
    //執行結果如下，可以發現intArray裡的元素竟然被更動了。
    //再對照前面的堆疊示意圖，只要算準回傳位址放在哪，再加以更改，整個動作就算完成了。*/
}