// java use keywords extends to show the relationship
// between super class(parent class) and subclass(child class)
public class Dog extends Animal{
    // redefine parent's method is called "override"
    public void move(){
        System.out.println("with legs");
    }
}